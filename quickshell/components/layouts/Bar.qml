import QtQuick
import Quickshell
import Quickshell.Wayland
import "../../" as Root

Rectangle {
    id: barRow
    anchors.top: parent.top
    anchors.left: parent.left
    anchors.right: parent.right
    height: 40
    color: "transparent"
}
